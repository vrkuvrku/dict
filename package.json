{
  "name": "dict",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "icons": "node tools/build-icons.mjs",
    "data": "node tools/build-data.mjs",
    "deploy": "npm run build && scp -r dist/* sarfy.cz:www/dict/"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "typescript": "^5.6.3",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^0.21.0"
  }
}
